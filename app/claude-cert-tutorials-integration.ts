export const integrationTutorials: Record<string, string> = {
  "ccar-p-006-enterprise-integration": `## 深入教程
### 1. Messages API 的责任边界
Claude 的 **Messages API（消息 API）** 提供模型调用，但应用仍负责构造每一轮消息、保存业务状态、执行工具、处理错误和控制权限。不要把聊天历史当成业务数据库，也不要把模型生成的“状态”当成已提交事实。

一次企业调用通常经过：入口认证 → 请求规范化 → 策略/租户上下文 → 检索与工具目录 → Claude → 输出验证 → 工具执行 → 结果持久化 → 评估与审计。每个边界都应有 request ID、用户/服务 principal、超时和错误语义。

### 2. API、MCP、CLI 与 Agent-to-Agent 怎么选
**Direct API（直接 API）** 适合应用拥有清晰服务契约、希望精确控制工具循环与运行环境的场景。优点是边界明确；代价是客户端需要管理状态、工具与恢复。

**Model Context Protocol, MCP（模型上下文协议）** 统一服务器连接、工具暴露和资源访问。Claude API 的 MCP connector 使用 **mcp_servers** 定义 URL 与认证，用 **mcp_toolset** 控制启用工具、缓存与 defer loading。MCP 提高复用性，但不会自动解决授权、工具质量或数据治理。

**CLI（命令行入口）** 适合开发者本地/CI 工作流，需要特别控制文件、命令、网络和凭据。**Agent-to-agent（代理到代理）** 只在跨系统确有独立代理责任时采用；必须定义身份传播、消息契约、重放、授权和最终 owner，不能把自然语言对话当可靠协议。

### 3. Tool contract 的组成
工具定义包括名称、详细 description、JSON input schema 和可选 input examples。工具描述应说明何时使用、何时不用、参数意义、返回内容、限制和副作用。对于支持的 schema，使用 **strict tool use（严格工具调用）** 保证名称和参数结构符合定义，但严格 schema 仍不能保证业务值正确或调用被授权。

模型输出 **tool_use** 只是请求。客户端执行工具，把每个结果以对应 **tool_result** 返回。错误应使用明确的 **is_error** 或结构化错误，不要把异常文本伪装成成功结果。并行工具调用的多个结果应在同一轮消息中完整返回。

### 4. 能力膨胀与渐进式发现
把几十或几百个工具定义全部放入上下文会造成 token 浪费和选择准确率下降。使用 **progressive discovery（渐进式发现）** 或 tool search：初始只加载搜索工具与少量核心工具，其余设为 deferred，按当前任务发现并展开。

渐进式发现也应遵循最小权限：发现目录不能让用户看到无权知道的资源名称；加载工具前再次按身份和租户过滤；执行时仍做策略校验。

### 5. 可靠的集成语义
对读取调用定义超时、缓存与新鲜度；对写调用定义 idempotency key、重复检测、状态查询和补偿。不能对未知完成状态的不可幂等写入直接重试。跨服务传递 deadline 而不是每一层独立使用相同超时，避免下游继续工作而上游已经放弃。

可观测字段至少包括：correlation ID、principal、tenant、模型/提示版本、工具名与版本、输入摘要、状态、延迟、token、费用、重试、策略结果和最终业务记录 ID。日志应脱敏，不能记录授权 token、密钥或无必要的完整提示。

### 6. 工单系统案例
Web 入口认证员工；编排器保存工单 ID 与会话摘要；知识 MCP 只暴露员工可访问文档；工单 API 工具分读/写两个权限；Claude 先检索并起草，若需要写入则生成严格结构化意图；策略层确认用户有编辑权限后执行；失败时保留草稿并返回可恢复状态。

### 7. 考试常见陷阱
- 认为 Messages API 会自动保存会话或业务状态。
- 把 MCP 当作权限系统，忽略每个工具的认证授权。
- 把 strict schema 当作业务正确性与安全保证。
- 为方便选择一次加载所有工具。
- 对超时后的写操作盲目重试。

## 练习参考答案
直接 API 适合团队拥有工单服务并需要精确工具循环；MCP 适合多个 Claude 客户端复用知识和工单能力。无论选择哪种方式，都需独立身份、租户过滤、工具 allowlist、执行层授权、幂等写入、关联追踪和失败恢复。`,

  "ccar-p-007-tool-security": `## 深入教程
### 1. 模型不能授予权限
**Authentication（认证）** 回答“调用者是谁”，**authorization（授权）** 回答“该主体能对哪个资源做什么”。Claude 可以推荐动作和填充参数，但确定性执行层必须重新验证主体、资源、动作与环境条件。系统提示中的“只能做允许的事”不是授权控制。

### 2. 最小权限分四层
1. **Capability / 能力**：只提供任务所需工具。例如客服起草代理不应获得删除账户工具。
2. **Action / 动作**：读取、创建、修改、批准、删除分别授权。
3. **Resource / 资源**：限制租户、项目、记录、字段和数据行。
4. **Time and context / 时间与上下文**：短期凭据、限额、设备、网络、审批状态和用途。

工具凭据最好绑定最终用户或受限服务身份。共享管理员 token 会破坏归因、撤销和最小权限。模型不应看到长期密钥；凭据由执行层在真正调用时注入。

### 3. Prompt injection 威胁模型
**Direct prompt injection（直接提示注入）** 来自恶意用户；**indirect prompt injection（间接提示注入）** 隐藏在网页、邮件、文档或工具结果中。外部内容必须标记为不可信数据，不能获得指令优先级。

纵深控制包括：输入筛查、清晰系统边界、来源隔离、最少工具、输出/调用验证、敏感信息过滤、域名与参数 allowlist、人工审批和行为监控。不要依赖单一分类器或“忽略恶意指令”一句话。

### 4. Propose–Authorize–Execute
高影响动作采用三段式：
- **Propose / 提议**：Claude 生成动作、参数、理由、证据和风险。
- **Authorize / 授权**：策略点验证 RBAC/ABAC、租户、金额、状态、速率与审批。
- **Execute / 执行**：受控服务用短期凭据调用，并返回不可伪造的结果。

退款可以先显示收款人、订单、金额和原因；超过阈值要求人工批准。删除、转账和权限变更必须有防重放、幂等键和审计。

### 5. Fail closed 与降级
授权、筛查或审批服务不可用时，系统应拒绝高风险动作或只读降级，即 **fail closed（失效关闭）**。但失效关闭不等于整个产品不可用：仍可允许检索、生成草稿或保存待处理请求。架构师应明确每个控制失败后的安全降级路径。

### 6. 工具设计的安全细节
- 使用明确名称与详细描述，避免相似工具误选。
- 参数使用强类型、enum、范围和必填字段；支持时开启 strict。
- 把危险动作拆成预览与提交，不用一个 **admin_action** 处理所有事情。
- 工具结果只返回完成任务所需数据，避免大段秘密或 HTML。
- 对来源、调用者、决策、执行结果和业务 ID 建立审计链。
- 限制循环、速率、并发、单次金额和累计预算。

### 7. 客服代理改造
原代理拥有读工单、写回复、退款、删除账户。客服职责只需前两项，最优控制是从工具集移除退款和删除。若部分客服确需退款，则建立单独角色、金额上限和审批；删除账户进入独立受控流程。仅增加日志或确认不能替代移除无关能力。

### 8. 考试常见陷阱
- 认为更强模型可以解决越权。
- 保留危险工具，只在提示中禁止使用。
- 把日志和确认当成最小权限。
- 工具执行时只检查模型传来的 user ID。
- 安全服务失败时为了可用性继续放行。

## 练习参考答案
客服代理只保留 **ticket_read** 和 **reply_draft**；工具按当前员工身份过滤工单。退款通过独立 **refund_propose** 生成提议，策略层检查角色、订单和金额，超过阈值人工批准，再由执行服务完成。账户删除完全不暴露给该代理。`,

  "ccar-p-008-rag": `## 深入教程
### 1. RAG 的完整数据链
**Retrieval-Augmented Generation, RAG（检索增强生成）** 不是“向量库 + 模型”两个组件，而是一条数据产品链：来源登记 → 权限与质量检查 → 解析 → 分块 → 元数据 → embedding/关键词索引 → 检索 → 过滤 → 重排 → 上下文组装 → Claude 生成 → 引用与验证 → 反馈。

任何一段失效都可能表现为“模型回答错误”。因此设计和评估必须分段。

### 2. 语料治理
每份文档应保留 source ID、标题、版本、owner、生效/失效时间、访问控制、语言和更新时间。刷新流程需要删除或标记旧版本，避免新旧政策同时被检索。若源文档没有权威性，RAG 只会更快地生成带证据的错误答案。

解析时检查扫描 PDF、表格、页眉页脚、列表和跨页段落。失败记录进入 quarantine，不能静默索引空文本。

### 3. Chunking 设计
固定字符切分简单，但容易割裂语义。优先按标题、段落、条款或会话轮次切分，并适度 overlap。chunk 太小会丢失上下文，太大会降低检索精度并增加 token。为每个 chunk 保存父文档、章节、页码、版本和访问标签。

Claude citations 支持文档与自定义内容。需要精确引用时，将 RAG chunk 作为可引用文档内容传入，并设计合适的引用粒度。引用存在不代表检索正确，仍需评价证据是否支持结论。

### 4. 检索策略
**Keyword/BM25（关键词检索）** 擅长产品编号、法规条款和专有名词；**vector search（向量检索）** 擅长语义相似表达；**hybrid search（混合检索）** 结合两者；**metadata filtering（元数据过滤）** 先限制租户、时间、地区和文档类型；**reranking（重排序）** 对候选结果再次排序。

Anthropic 不提供自有 embedding 模型，因此需评估外部供应方的质量、语言、维度、费用、数据处理和部署边界。embedding 与索引版本必须可追踪，升级时重新建立并回归评估。

### 5. Context assembly 与回答策略
检索结果应去重、排序、保留来源，并限制总 token。系统提示要求 Claude 只依据证据回答，区分事实、推断和未知。没有足够证据时拒答或请求澄清；证据冲突时展示冲突和版本，不要让模型擅自选择。

### 6. 分层评估
- Corpus：覆盖率、最新性、权限标签正确率。
- Parsing/chunking：文本完整率、语义边界与元数据。
- Retrieval：Recall@K、Precision@K、MRR、nDCG 和无结果率。
- Generation：事实支持率、引用正确率、完整性、拒答质量。
- End-to-end：任务成功、人工节省、延迟、成本与安全。

建立失败分类：正确 chunk 未检索、检索到错误 chunk、正确证据被忽略、证据不足仍作答、引用不支持结论。不同类型的修复位置不同。

### 7. 文档刷新故障案例
刷新后错误突然增加，模型与延迟未变。先验证新文档是否成功解析、旧 chunk 是否删除、索引任务是否完成、过滤条件是否仍匹配、返回的 top-K 是否相关。直接换更强模型或调整 temperature 不能修复陈旧索引。

### 8. 考试常见陷阱
- 把更多 top-K 当作必然更好。
- 只测最终答案，不测 retrieval recall。
- 忽略文档版本、ACL 和删除传播。
- 认为有引用就没有 hallucination。
- 把所有问题都用向量检索处理。

## 练习参考答案
50 万页政策库按文档类型解析，按章节/条款分块并保留版本、地区、ACL 与生效时间；先做权限和元数据过滤，再用混合检索与重排；Claude 只基于返回证据回答并输出引用；刷新时双索引构建、完整性校验、切换和回滚；分别测 Recall@K、引用支持率、拒答和端到端任务成功。`,

  "ccar-p-009-evaluation": `## 深入教程
### 1. 从成功标准开始
官方提示工程流程把 **success criteria（成功标准）** 和 eval 放在优化之前。先写“什么叫好”，再写提示。指标应具备业务相关性、可测量性、代表性和可分解性。

例如客服系统不能只测“回答是否好”：还要测解决率、事实正确、政策遵循、引用、升级判断、p95 延迟、成本、拒答、安全和客户满意度。不同指标的重要性取决于失败代价。

### 2. 指标类型
- **Task success / 任务成功**：用户目标是否完成。
- **Correctness / 正确性**：事实和结构字段是否正确。
- **Groundedness / 有据性**：结论是否被提供证据支持。
- **Safety/security / 安全**：越权、注入、敏感数据和政策违规。
- **Operational / 运营**：延迟、可用性、token、成本和工具成功。
- **Business / 业务**：节省时间、采纳、转化、返工和事故。

安全与合规通常是硬门禁，不能被平均质量分抵消。

### 3. 构建数据集
从真实分布抽样，并加入边界、历史故障、罕见高风险、对抗、长输入、多语言和无答案样本。为每条记录保存输入、期望行为、可接受答案、证据、风险标签与评分规则。

划分 development set、validation set 和 **holdout set（保留集）**。开发过程中可以看开发集错误，但不能不断针对保留集改提示，否则会评估过拟合。数据集也应版本化并检查敏感信息。

### 4. 四类评分器
1. **Deterministic grader（确定性评分）**：exact match、schema、正则、计算、引用 ID，稳定便宜。
2. **Model-based grader（模型评分器）**：适合语义、风格与复杂 rubric，但需校准偏差。
3. **Human expert（人工专家）**：适合高风险、主观和新型失败，成本较高。
4. **Production signal（生产信号）**：采纳、修订、升级、投诉和任务完成，最贴近业务但受外部因素影响。

使用 LLM-as-a-judge 时，给出清晰 rubric、证据与示例；让评分器输出分项和理由；与盲法人工标注比较一致率；避免让被评模型和评分器共享偏差成为唯一证据。

### 5. 发布门禁
一个示例 gate：严重政策违规 = 0；严重风险漏报 < 1%；引用支持率 ≥ 97%；任务成功 ≥ 88%；相对基线质量不下降；p95 < 4 秒；单任务成本 ≤ 预算。门禁应指定样本量、置信区间、owner 和失败处理。

A/B 测试只能在离线安全门禁通过后进行。高风险场景先 shadow 或受控 pilot，不直接把未验证模型暴露给所有用户。

### 6. 评估闭环
生产错误脱敏后进入错误分类和候选数据集；定期审查漂移；模型、提示、检索、工具和策略任一变更都触发相关回归。不要只看平均分，要按语言、任务、风险、租户和输入长度切片。

### 7. 考试常见陷阱
- 提示写完才考虑如何评价。
- 只用 happy path 或合成数据。
- 用一个综合平均分掩盖安全失败。
- 未校准就完全依赖 LLM judge。
- 把在线 A/B 当作发现严重安全问题的第一道测试。

## 练习参考答案
客服指标可包括事实正确率、引用支持率、解决率、正确升级率、严重政策违规、敏感数据泄漏、p95 延迟和单次成本。硬门禁可设严重违规为 0、关键事实正确率 ≥ 98%；可优化指标包括平均响应时间和单次成本，但优化后仍需保持质量门槛。`,

  "ccar-p-010-optimization-observability": `## 深入教程
### 1. 建立分层测试金字塔
最底层使用确定性测试检查模板、schema、路由、权限、解析和工具适配器；组件层单独测试 prompt、retrieval、tool selection 与 guardrail；系统层运行端到端业务任务；生产前加入负载、超时、429/529、工具失败、陈旧索引和安全对抗测试。

模型输出具有非确定性，因此测试不应要求所有自然语言完全一致，而应验证结构、事实、证据、政策和可接受范围。

### 2. 错误分类与定位
当答案错误时按顺序问：
1. 输入与用户意图是否正确规范化？
2. 路由是否进入正确流程、模型和工具集？
3. 所需事实是否在上下文或检索结果中？
4. 工具是否被正确调用并返回新鲜、完整结果？
5. 系统提示和示例是否清晰且无冲突？
6. 模型是否忽略证据、错误推断或能力不足？
7. 输出解析、过滤或下游映射是否破坏结果？

只有确认根因在模型能力时才升级模型。很多“hallucination”实际是错误检索、陈旧数据或输出后处理。

### 3. 质量约束下的优化顺序
先做不伤质量的优化：移除重复/无关 token、缓存稳定前缀、并行独立任务、减少无意义轮次、压缩工具返回、对可等待任务使用 Message Batches。批处理适合大规模 eval 和离线分析，标准价格通常低于同步调用，但不能用于要求即时响应的路径。

再测试有 trade-off 的优化：更快/更小模型、调整 effort、减少 top-K、限制输出、降低代理步数、模型路由。每项变化单独实验并回归所有硬门禁。

### 4. Prompt caching 观测
记录 cache creation、cache read、uncached input、输出 token、命中率和延迟。缓存失效时比较模型、工具定义、tool choice、thinking/effort 和断点之前的内容是否变化。缓存降低重复前缀处理，不保证应用结果正确，也不减少动态后缀与输出费用。

### 5. 可观测数据模型
每个端到端 trace 应能关联：
- request、session、user/tenant 和 business ID。
- 模型 ID、提示版本、参数与停止原因。
- 输入/输出 token、缓存、费用和速率限制。
- 检索查询、索引版本、候选与选择结果。
- 工具名、参数摘要、授权决定、延迟、错误与重试。
- guardrail、人工审批和最终 eval/业务反馈。

使用字段 allowlist、脱敏、哈希和受控采样保护隐私。日志不是把完整提示永久保存；保留策略应与数据分类一致。

### 6. SLO 与告警
区分平台可用、任务成功和质量 SLO。API 仍返回 200 时，任务成功率或引用质量可能已经下降。告警要有阈值、窗口、严重级别、owner 与 runbook；同时监控模型供应方错误率、token/费用异常、工具失败和索引新鲜度。

### 7. 事件案例
“回答错误但 HTTP 200”先查看 trace。若返回 chunk 与问题无关，修 retrieval；若 chunk 正确但回答不支持，检查提示和模型；若工具返回错误值，修集成；若模型输出正确但字段映射错误，修后处理。每次事故产生新的回归样本和监控信号。

### 8. 考试常见陷阱
- 先换大模型，再调查数据与工具。
- 只监控 HTTP 状态和平均延迟。
- 优化成本后不重跑安全与质量评估。
- 在日志中记录完整秘密和敏感内容。
- 将缓存 TTL 误认为端到端数据新鲜度。

## 练习参考答案
诊断树依次分为输入/路由、检索、提示/上下文、模型、工具、guardrail 和输出后处理。每层用 trace、版本、输入输出与组件指标判断；修复后把该事件加入回归集，并为可提前发现的信号建立告警。`,
};
