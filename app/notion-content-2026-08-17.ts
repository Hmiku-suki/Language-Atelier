export const notionTopicContent20260817 = {
  "jp-explain-idempotency-prevent-duplicate-processing": {
    title: "重複処理を防ぐための冪等性を説明する",
    sourceId: "3bfd34cbae198123b865e05e88fab472",
    markdown: "## 今日のテーマと学習目標\n今天学习在接口设计、批量处理和故障恢复中，如何用日语说明“同一个业务操作即使被重复发送，也不会产生重复结果”的设计原则。\n学习目标：\n- 区分「再送」「再試行」「重複実行」和「二重登録」\n- 说明超时只代表没有收到结果，不一定代表处理失败\n- 明确什么条件下两次请求被视为同一个业务操作\n- 说明幂等键、处理记录、并发控制和结果复用\n- 区分单条请求的幂等性与批量文件中逐条数据的幂等性\n## 実用的な語彙・表現\n### 1. 冪等性\n**读音：** べきとうせい  \n**中文：** 幂等性  \n**语感与场景：** 表示相同操作执行一次或多次，最终业务状态不会因重复执行而继续变化。技术会议中常直接使用。\n> タイムアウト後に安全に再送できるよう、このAPIには冪等性を持たせる必要があります。\n为了在超时后能够安全重发，需要让这个 API 具备幂等性。\n### 2. 重複実行\n**读音：** ちょうふくじっこう  \n**中文：** 重复执行  \n**语感与场景：** 指同一业务处理被执行两次以上。原因可能是重试、重复消息、并发或人工操作。\n> 同じファイルが再送された場合でも、登録処理が重複実行されないようにします。\n即使同一文件被重新发送，也要避免注册处理被重复执行。\n### 3. 再送\n**读音：** さいそう  \n**中文：** 重新发送  \n**语感与场景：** 强调把请求、消息或文件再次发送。邮件、API 和 SFTP 文件都可以使用。\n> 応答が返らなかったため、送信側から同じデータが再送される可能性があります。\n由于没有收到响应，发送方可能会重新发送相同数据。\n### 4. 再試行\n**读音：** さいしこう  \n**中文：** 重试、再次尝试  \n**语感与场景：** 强调再次执行失败或未完成的操作。口语中也常说「リトライ」。\n> 再試行の回数だけでなく、間隔と打ち切り条件も定義してください。\n不仅要定义重试次数，还应定义间隔和停止条件。\n### 5. 一意な識別子\n**读音：** いちいなしきべつし  \n**中文：** 唯一标识符  \n**语感与场景：** 用来识别一个业务操作或一条记录。只说「ID」时可能不清楚是技术请求还是业务数据的 ID。\n> 送信元で一意な処理識別子を採番し、再送時にも同じ値を使用します。\n由发送方生成唯一处理标识符，并在重发时继续使用相同的值。\n### 6. 重複を検知する\n**读音：** ちょうふくをけんちする  \n**中文：** 检测到重复  \n**语感与场景：** 「検知する」比「見つける」正式，常用于系统自动识别异常或重复状态。\n> 受信側は処理識別子と対象データを照合し、重複を検知します。\n接收方会核对处理标识符和目标数据，检测重复请求。\n### 7. 処理済み\n**读音：** しょりずみ  \n**中文：** 已处理  \n**语感与场景：** 「～済み」表示动作已经完成。设计时还需明确是“接收完成”还是“业务更新完成”。\n> 処理済みの識別子であれば、新たに更新せず、前回の結果を返します。\n如果该标识符已经处理过，就不再次更新，而是返回上次结果。\n### 8. 二重登録\n**读音：** にじゅうとうろく  \n**中文：** 重复登记、重复创建  \n**语感与场景：** 强调同一业务对象被创建两次，常见于订单、付款、员工关系或申请记录。\n> ネットワーク障害時の再送によって、同じ申請が二重登録されました。\n由于网络故障后的重发，同一申请被重复登记了。\n### 9. 副作用\n**读音：** ふくさよう  \n**中文：** 副作用；操作对外部状态造成的变化  \n**语感与场景：** 在软件设计中包括数据库更新、发邮件、扣款和向外部系统发送消息，并非医学含义。\n> データベース更新だけでなく、メール送信などの副作用も重複しないように設計します。\n不仅数据库更新，发送邮件等副作用也要避免重复发生。\n### 10. 整合性を保つ\n**读音：** せいごうせいをたもつ  \n**中文：** 保持一致性  \n**语感与场景：** 表示多个状态或数据之间没有矛盾。比笼统的「データを正しくする」更专业。\n> 並行して同じ要求を受信しても、処理記録と業務データの整合性を保つ必要があります。\n即使并发收到相同请求，也必须保持处理记录与业务数据的一致性。\n### 11. 前回の結果を再利用する\n**读音：** ぜんかいのけっかをさいりようする  \n**中文：** 复用上一次的处理结果  \n**语感与场景：** 检测到重复后，不一定只返回“已忽略”，也可以返回首次执行时保存的业务结果。\n> 重複要求には、初回処理で保存した結果を再利用して応答します。\n对于重复请求，将复用首次处理时保存的结果进行响应。\n### 12. 結果が確定していない\n**读音：** けっかがかくていしていない  \n**中文：** 结果尚未确定  \n**语感与场景：** 超时或连接中断时的准确说法。此时不能直接断定成功或失败。\n> タイムアウトした時点では、相手側の更新結果が確定していません。\n发生超时时，尚不能确定对方系统的更新结果。\n## 高階文法・文型\n### 1. ～たところで\n表示即使执行前项，也不会改变后项结果，常含“做了也没有意义或不会产生额外效果”的语气。\n> 同じ識別子で何度送信したところで、業務データは一度しか更新されません。\n即使使用相同标识符发送多次，业务数据也只会更新一次。\n与「～ても」相比：\n- 「～ても」广泛表示让步条件\n- 「～たところで」更强调即使实际执行，结果仍不会改变\n- 它有时带消极语感，但在说明幂等行为时可以客观使用\n### 2. ～ない限り\n表示只要某个必要条件不成立，后项就不会发生。\n> 処理識別子と内容の両方が一致しない限り、同じ要求とはみなしません。\n只要处理标识符和内容没有同时一致，就不视为同一个请求。\n与「～なければ」相比：\n- 「～なければ」是一般条件\n- 「～ない限り」强调前项是后项成立的必要边界\n- 适合说明设计规则、例外条件和安全限制\n### 3. ～とみなす\n表示按照规则把某个状态判定为另一种状态，常用于规格、合同和正式说明。\n> 同一の処理識別子と同一のペイロードを受信した場合は、再送とみなします。\n收到相同处理标识符和相同载荷时，将其视为重发。\n与「～と考える」相比：\n- 「～と考える」表示说话人的判断或分析\n- 「～とみなす」表示系统或规则正式采用的判定\n- 使用时必须同时说明判定条件\n### 4. ～ものとする\n用于规格书或规则中正式规定系统行为，语气比普通说明更强。\n> 同じ識別子で異なる内容を受信した場合は、入力不整合としてエラーにするものとします。\n若使用同一标识符收到不同内容，则规定将其作为输入不一致错误处理。\n与「～こととする」相比：\n- 两者都能规定规则\n- 「～ものとする」常用于系统行为、合同和客观规范\n- 「～こととする」也可用于团队决定或行动方针，如「今後は二名で確認することとする」\n- 日常对话中不必刻意使用，设计书和会议结论中更合适\n## 自然な会話\n**场景：SuccessFactors 更新接口在响应前发生超时，发送方准备自动重发同一批数据。**\n**開発担当：**\n> 更新APIがタイムアウトした場合は、エラーとして同じリクエストを三回リトライする予定です。\n**アーキテクト：**\n> タイムアウトしたからといって、相手側の処理が失敗したとは限りません。更新は完了していて、応答だけが届かなかった可能性もあります。\n**開発担当：**\n> では、自動リトライは行わないほうがよいでしょうか。\n**アーキテクト：**\n> 再試行自体を禁止する必要はありません。安全に再送できるよう、まず冪等性を設計しましょう。同じ業務操作を何で識別しますか。\n**開発担当：**\n> ファイル名と社員番号の組み合わせではどうでしょうか。\n**アーキテクト：**\n> ファイル名が再生成される可能性がありますし、一人に複数の更新がある場合もあります。送信元で一意な処理識別子を採番し、再送時には同じ値を使うほうが明確です。\n**開発担当：**\n> 受信側では、その識別子が処理済みか確認するということですね。\n**アーキテクト：**\n> はい。ただし、識別子だけでなく内容も照合してください。同じ識別子と同じ内容なら再送とみなし、前回の結果を返します。同じ識別子なのに内容が異なる場合は、誤用の可能性があるためエラーにします。\n**開発担当：**\n> バッチ内で一部だけ成功した場合はどうしますか。\n**アーキテクト：**\n> ファイル全体の状態だけでは不十分です。各レコードにも安定した業務キーまたは明細識別子を持たせ、成功済みの明細は再実行せず、未完了の明細だけを再処理できるようにしましょう。\n**開発担当：**\n> 一意制約を設定すれば、二重登録は防げますか。\n**アーキテクト：**\n> データベースの重複は防げますが、その前に送信したメールや外部API呼び出しまで自動的に元に戻るわけではありません。副作用を含めて、どこまでを同じ処理として管理するか決める必要があります。\n**開発担当：**\n> 処理記録の保存期間と、同時に同じ要求が来た場合の排他制御も設計書に追記します。\n## よくある中式・不自然な表現の修正\n### 1. 超时后立即断定失败\n❌ タイムアウトしたので、更新は失敗しました。\n✅ タイムアウトしたため、現時点では更新結果を確認できていません。  \n✅ 更新が完了し、応答だけが届かなかった可能性もあります。\n超时表示调用方未在规定时间内收到结果，不足以证明对方没有执行。\n### 2. 把「再送」和「再試行」完全混用\n△ エラーになったので、ファイルを再試行します。\n✅ エラーになったため、同じファイルを再送します。  \n✅ 失敗した処理を一定時間後に再試行します。\n文件和消息通常「再送する」；操作或处理通常「再試行する」。\n### 3. 只说“重复数据就忽略”\n❌ 重複データは無視します。\n✅ 処理識別子と内容が一致する場合は再送とみなし、新たな更新は行わず、前回の処理結果を返します。\n「無視する」没有说明如何判断重复、调用方会收到什么结果，也不利于审计。\n### 4. 认为加上唯一约束就完全安全\n❌ DBに一意制約があるので、冪等性は問題ありません。\n✅ 一意制約で二重登録は防げますが、メール送信や外部API呼び出しなどの副作用は別途制御が必要です。\n数据库约束只能保护其覆盖的写入，无法自动保护所有外部副作用。\n### 5. “再执行也是一样”过于模糊\n❌ もう一度実行しても同じです。\n✅ 同じ処理識別子と同じ内容で再送した場合、業務データは再更新せず、初回と同じ結果を返します。\n应该明确“什么相同”“什么不会重复”以及“返回什么”。\n### 6. 把重试次数当作安全保证\n❌ 三回までのリトライなら安全です。\n✅ 再試行回数にかかわらず、更新処理の冪等性と打ち切り条件を定義する必要があります。\n重复两次也可能产生错误结果；安全性不由次数决定。\n## 口頭・作文練習\n请根据以下条件，用日语进行约60秒的设计评审说明：\n- 外部系统更新后可能在返回响应前超时\n- 超时时结果不明，不能立即视为失败\n- 发送方为每个业务操作生成唯一处理标识符\n- 重发时必须使用相同标识符\n- 相同标识符且内容相同：不再次更新，返回上次结果\n- 相同标识符但内容不同：作为输入不一致错误拒绝\n- 批量处理中按明细记录处理状态\n- 已成功明细不再执行，只重试未完成明细\n- 数据库唯一约束不能防止邮件或外部调用重复\n- 处理记录的保存期应长于允许重试的时间范围\n- 监控重复检测数和异常的标识符复用\n请至少使用以下三个表达：\n- 冪等性\n- 一意な識別子\n- 処理済み\n- 副作用\n- ～たところで\n- ～ない限り\n- ～とみなす\n- ～ものとする\n## 参考答案\n> 外部システムの更新では、処理完了後に応答だけがタイムアウトする可能性があります。そのため、タイムアウトを直ちに失敗とみなして新しい要求を送ると、二重登録につながります。送信側は業務操作ごとに一意な処理識別子を採番し、再送時にも同じ値を使用するものとします。受信側では、識別子と内容の両方が一致する場合を再送とみなし、処理済みであれば新たに更新せず、前回の結果を返します。同じ識別子で内容が異なる場合は、入力不整合として拒否します。バッチ処理では明細単位で状態を記録し、成功済みの明細は再実行せず、未完了分だけを再試行します。データベースの一意制約だけではメールや外部APIなどの副作用を防げないため、それらも同じ処理境界で管理します。処理記録は許容する再試行期間より長く保存し、重複検知数と不正な識別子再利用を監視します。\n## 今日のポイント\n- 超时后的准确状态是：**失败已确认**ではなく、**結果が確定していない**\n- 幂等性必须先定义“同一个操作”的识别边界，不能只说“重复就忽略”\n- 推荐规则：**同一标识符＋同一内容＝重发；同一标识符＋不同内容＝错误**\n- 批量文件的接收成功，不等于每条明细都成功；恢复通常需要明细级状态\n- 唯一约束可以防止部分数据库重复，但不能自动阻止邮件、扣款或外部 API 等副作用\n- 处理记录保存期必须覆盖可能发生重发的时间窗口\n- 安全的重试需要同时考虑：**幂等性、并发控制、停止条件、可观察性和人工恢复路径**",
  },
  "en-safe-scheduled-jobs-distributed-coordination": {
    title: "Designing Safe Scheduled Jobs and Distributed Coordination",
    sourceId: "3bfd34cbae19818a96c1d9b13ded8f72",
    markdown: "Today’s focus: ensuring scheduled and singleton workloads remain safe when processes restart, nodes disagree, locks expire, or executions overlap.\n## Architecture Review\n**Less precise**\n> The scheduler runs this job only once, so duplicate execution cannot happen.\n**Senior-architect phrasing**\n> A single scheduling trigger does not guarantee single execution. Delivery retries, failover, lease expiry, manual reruns, and delayed workers can all create overlapping attempts.\n> Before approving the design, we should define the coordination scope, lease semantics, execution identity, overlap policy, fencing mechanism, catch-up behavior, and recovery procedure.\n> Correctness should not depend solely on the coordinator. The business operation should also be idempotent or protected by an authoritative state transition.\n## Technical Design Discussion\n> Give every logical run a stable execution ID derived from the schedule and workload scope. Retries of the same run must reuse that identity.\n> Use a time-bounded lease rather than an indefinite lock. The holder should renew it only while it remains healthy and must stop when renewal is no longer confirmed.\n> A lease alone cannot prevent a paused former holder from resuming after expiration. Issue a monotonically increasing fencing token and require the protected resource to reject commands carrying an older token.\n> Define whether overlapping runs are forbidden, queued, coalesced, or allowed for separate partitions. Make the decision explicit for each workload.\n> Separate trigger time from business time. A delayed run should process the intended business interval rather than silently switching to the current clock.\n> Define the misfire policy: skip missed intervals, execute the latest one, replay every interval, or aggregate them into one bounded catch-up run.\n> Record scheduled time, actual start time, execution ID, lease owner, fencing token, attempt, covered business interval, and terminal outcome.\n## Incident / Debugging\n**Less natural**\n> Two servers ran the cron at the same time because the lock did not work.\n**More natural**\n> The original worker paused during a long garbage-collection cycle and stopped renewing its lease. A second worker acquired a new lease, but the original worker later resumed and continued writing with stale ownership. Both attempts processed the same business interval.\n> Let’s reconstruct the lease timeline, renewal failures, fencing tokens, worker pauses, execution IDs, database commits, and downstream side effects.\n> The immediate mitigation is to suspend new triggers, fence stale workers, identify overlapping execution intervals, and reconcile affected records before resuming the schedule.\n## Stakeholder Update\n> A scheduled settlement job executed concurrently on two workers after the original worker lost its lease and later resumed. We have stopped additional runs and blocked writes from stale owners. The team is reconciling the affected settlement interval; customer-facing services remain available. We will resume the schedule after validating the corrected ownership checks and confirming that duplicate side effects have been contained.\n## Code Review / Mentoring\n> This code acquires a distributed lock but never verifies ownership again. Renew the lease while active and require a fencing token on every protected write.\n> The lock key is global, although the workload is partitioned by tenant. Choose the coordination scope deliberately; an unnecessarily global lock reduces throughput and increases recovery time.\n> This handler uses the current timestamp after a delayed start. Pass the scheduled business interval explicitly so retries and catch-up runs process the same data window.\n> Do not mark the run complete before downstream side effects are durable. Persist an auditable outcome and make retry behavior depend on that state.\n## Corrections\n❌ The distributed lock guarantees that only one worker can ever run.\n✅ A lease limits concurrent ownership, but stale workers must also be fenced from protected resources.\n❌ If the lock expires, the old worker will stop automatically.\n✅ Expiration changes coordinator state; it does not terminate or pause the former holder.\n❌ We can rerun the failed schedule with the current time.\n✅ A retry should retain the original execution identity and intended business interval unless an explicit recovery policy says otherwise.\n❌ Missed runs should always be replayed.\n✅ Catch-up behavior depends on workload semantics, backlog capacity, ordering requirements, and whether old intervals remain meaningful.\n## Vocabulary Notes\n- **scheduled workload**：定时工作负载；由时间计划触发的批处理或后台任务\n- **single-active execution**：单活执行；同一协调范围内只允许一个有效执行者\n- **lease**：租约；在有限时间内授予、需要续期的执行权\n- **lease renewal**：租约续期；执行者在健康状态下延长持有期限\n- **lease holder**：租约持有者；当前被协调系统认可的执行者\n- **fencing token**：隔离令牌；单调递增的所有权版本，用于拒绝旧持有者的写入\n- **stale owner**：过期所有者；已经失去执行权但仍可能继续运行的旧实例\n- **overlapping execution**：重叠执行；多个任务实例同时处理相同范围\n- **execution identity**：执行标识；稳定识别一次逻辑调度运行的ID\n- **misfire policy**：错过触发策略；规定调度未按时执行后的跳过、补跑或合并行为\n- **catch-up run**：补跑；对错过的业务时间段进行恢复处理\n- **business interval**：业务时间区间；本次任务应处理的数据窗口，而非实际启动时间\n- **clock skew**：时钟偏差；不同节点对当前时间的认知不一致\n- **split brain**：脑裂；多个节点同时认为自己拥有有效控制权\n## Speaking Drill\n用约 45 秒回答：\n> A nightly settlement job uses a distributed lease. The first worker paused, its lease expired, and another worker started. The first worker then resumed, so both processed the same interval. How would you contain the incident and redesign the coordination model?\n**Model answer**\n> I would suspend new triggers, invalidate the stale worker’s authority, and reconcile the settlement interval using execution IDs and downstream records. Lease expiry alone is insufficient because it does not stop the former holder. I would issue a monotonically increasing fencing token whenever ownership changes and require every protected write to reject older tokens. Each logical run should retain a stable execution ID and business interval across retries. I would also define renewal failure behavior, overlap and misfire policies, bounded catch-up, and telemetry for lease ownership, token version, attempts, and terminal outcomes.",
  },
} as const;

