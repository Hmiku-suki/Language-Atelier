export const notionTopicContent20260826 = {
  "jp-request-post-etl-sample": {
    title: "変換前後のデータ差分を確認し再提供を依頼する",
    sourceId: "3c8d34cbae1981a3a3e1d777faca9bef",
    markdown: "## 今日のテーマと学習目標\n今天学习在接口测试中收到测试文件后，如何准确说明“业务数据内容没有明显问题，但文件仍是 ETL 转换前格式”，并礼貌请求对方重新提供转换后的文件，或提出在连通性测试中连同 ETL 一起验证的替代方案。\n学习目标：\n- 区分业务值确认、文件格式确认和实际读入验证\n- 准确说明转换前后在哪些方面存在差异\n- 不用笼统的「形式が違います」结束报告\n- 在提出重新提供请求时说明必要性和后续影响\n- 同时给出可执行的替代验证方案\n## 実用的な語彙・表現\n### 1. 変換前／変換後\n**读音：** へんかんまえ／へんかんご  \n**中文：** 转换前／转换后  \n**语感与场景：** 用于明确文件处于处理链路中的哪个阶段。只说「元ファイル」可能无法判断是业务原始数据还是上游系统输出。\n> 今回受領したファイルは、ETLによる変換前の形式と認識しています。\n我方理解本次收到的文件仍是 ETL 转换前的格式。\n### 2. 前処理\n**读音：** まえしょり  \n**中文：** 前置处理、预处理  \n**语感与场景：** 指主接口读取前进行的字段转换、代码替换、格式统一、过滤或排序等处理。\n> IFへ連携する前に、ETLで項目名とコード値を変換する前処理があります。\n文件传给 IF 前，需要通过 ETL 对字段名和代码值进行预处理。\n### 3. 受信形式\n**读音：** じゅしんけいしき  \n**中文：** 接收方实际接收的数据格式  \n**语感与场景：** 不仅指 CSV、XML 等大类，也包括字符编码、分隔符、字段顺序和换行方式。\n> IFが想定している受信形式と一致しているかを確認します。\n需要确认文件是否与 IF 所预期的接收格式一致。\n### 4. 想定形式\n**读音：** そうていけいしき  \n**中文：** 设计或实现中预期的格式  \n**语感与场景：** 「想定」表示设计时采用的前提。最好同时说明该前提来自设计书、样本还是现有程序。\n> 設計書上の想定形式は、ETL変換後のCSVファイルです。\n设计书中预期的是经过 ETL 转换后的 CSV 文件。\n### 5. 項目構成\n**读音：** こうもくこうせい  \n**中文：** 字段构成、字段结构  \n**语感与场景：** 包括字段数量、名称、顺序、是否必填等。比单说「項目」更强调整体结构。\n> 値の内容に問題はありませんが、項目構成はIFの入力仕様と異なっています。\n数据值本身没有问题，但字段构成与 IF 的输入规格不同。\n### 6. 差分を洗い出す\n**读音：** さぶんをあらいだす  \n**中文：** 全面找出差异  \n**语感与场景：** 「洗い出す」表示不遗漏地列举。常用于规格比较、调查和影响分析。\n> 変換前後で、項目名、桁数、コード値の差分を洗い出します。\n需要全面找出转换前后在字段名、位数和代码值方面的差异。\n### 7. 整合する\n**读音：** せいごうする  \n**中文：** 相互一致、符合  \n**语感与场景：** 常说「仕様と整合する」「設計書と整合している」。不是一般意义上的“整理”。\n> 提供いただいたサンプルが最新のレイアウト定義と整合しているか確認しました。\n我们确认了所提供的样本是否与最新布局定义一致。\n### 8. 取り込み可否\n**读音：** とりこみかひ  \n**中文：** 能否被系统导入  \n**语感与场景：** 正式、简洁，常用于测试结果和确认事项。口语也可以说「取り込めるかどうか」。\n> 実際の取り込み可否は、変換後ファイルを使わないと判断できません。\n如果不使用转换后的文件，就无法判断实际上能否导入。\n### 9. 再提供を依頼する\n**读音：** さいていきょうをいらいする  \n**中文：** 请求重新提供  \n**语感与场景：** 「再送」偏向再次发送同一文件；如果内容或格式需要改变，应使用「再提供」更准确。\n> お手数ですが、ETL変換後の形式で再提供をお願いできますでしょうか。\n麻烦您能否按照 ETL 转换后的格式重新提供？\n### 10. 疎通試験\n**读音：** そつうしけん  \n**中文：** 连通性测试  \n**语感与场景：** 主要验证系统之间能否连接和传输，但项目中也应明确本次是否包含格式转换和数据读入。\n> 来週の疎通試験では、ETLからIFまでを一連の処理として確認します。\n下周的连通性测试将把 ETL 到 IF 作为一整套流程进行确认。\n### 11. 併せて確認する\n**读音：** あわせてかくにんする  \n**中文：** 一并确认  \n**语感与场景：** 表示把相关事项放在同一测试或调查中处理。比「一緒に見る」正式。\n> 変換後ファイルを事前に用意できない場合は、疎通試験の中で併せて確認します。\n如果无法事先准备转换后的文件，就在连通性测试中一并确认。\n### 12. 代替案\n**读音：** だいたいあん  \n**中文：** 替代方案  \n**语感与场景：** 无法满足理想方案时提出的可行路径。应说明替代方案能验证什么、仍有哪些风险。\n> 再提供が難しい場合の代替案として、ETLを含めた結合確認を提案します。\n如果难以重新提供，作为替代方案，建议进行包含 ETL 的集成确认。\n## 高階文法・文型\n### 1. ～に沿った\n表示按照规则、方针、规格或既定路线进行。\n> IFの入力仕様に沿った変換後ファイルをご提供ください。\n请提供符合 IF 输入规格的转换后文件。\n与「～に基づいた」相比：\n- 「～に沿った」强调不偏离方针、流程或格式要求\n- 「～に基づいた」强调结论或内容以资料、数据或规则为依据\n- 文件应“符合规格”使用「仕様に沿った」；设计判断“依据规格”可使用「仕様に基づいた判断」\n### 2. ～ないことには\n表示如果前项必要条件不能实现，就无法进行后项，语气比普通的「～なければ」更强调现实上的困难。\n> 変換後の項目構成を確認しないことには、IFで取り込めるか判断できません。\n如果不确认转换后的字段结构，就无法判断 IF 能否导入。\n与「～ない限り」相比：\n- 两者都能表示必要条件\n- 「～ないことには」常表示由于缺少前提，现实中无法判断或行动\n- 「～ない限り」更强调只要条件不改变，后项状态就持续不成立\n- 调查材料不足时，「確認しないことには判断できない」很自然\n### 3. ～に先立って\n表示在正式活动或重要步骤开始之前先做某事，属于书面和商务表达。\n> 来週の疎通試験に先立って、変換後ファイルを事前に確認したいと考えています。\n我们希望在下周连通性测试之前，预先确认转换后的文件。\n与「～前に」相比：\n- 「～前に」是普通的时间先后表达\n- 「～に先立って」强调前项是为重要活动做准备\n- 测试、发布、会议和迁移等正式活动适合使用「～に先立って」\n### 4. ～ていただくことは可能でしょうか\n用于郑重询问对方是否能够采取某项行动，重点是确认现实可行性。\n> ETL変換後のサンプルを一件だけご提供いただくことは可能でしょうか。\n请问能否提供一份经过 ETL 转换的样本？\n与「～ていただけますか」相比：\n- 两者都可提出礼貌请求\n- 「～ていただけますか」直接请求对方行动\n- 「～ていただくことは可能でしょうか」更强调对方的条件和可行性\n- 简单日常请求不必使用过长表达；涉及跨团队准备和进度调整时比较合适\n## 自然な会話\n**场景：客户提供了接口测试数据。业务字段值已确认，但文件是 ETL 转换前格式，而 IF 实际读取的是转换后文件。**\n**お客様：**\n> 先日お送りしたテストデータについて、確認状況はいかがでしょうか。\n**IF担当：**\n> ご提供いただいたデータは確認済みです。社員番号やコード値など、データの内容面では現時点で大きな問題は見当たりませんでした。\n**お客様：**\n> では、このファイルを使って疎通試験を進められますか。\n**IF担当：**\n> 一点、形式について確認が必要です。今回いただいたファイルはETL変換前のレイアウトですが、IFが実際に読み込むのは変換後のファイルです。\n**お客様：**\n> 具体的には、どこが違いますか。\n**IF担当：**\n> 変換後は項目名、項目順、コード値の一部が変わる想定です。また、区切り文字と空項目の表現もETL側で統一されます。現在のファイルでは業務値は確認できますが、IFへの取り込み可否までは判断できません。\n**お客様：**\n> ETL変換後のサンプルは、まだ用意できていないようです。\n**IF担当：**\n> 承知しました。可能であれば、来週の疎通試験に先立って、一件だけでも変換後ファイルをご提供いただくことは可能でしょうか。Group側でも送信前に形式を確認したいと聞いています。\n**お客様：**\n> 事前提供が難しい場合、疎通試験の当日にETLから流して確認する形でもよいですか。\n**IF担当：**\n> はい、代替案としては可能です。その場合は、ETL変換、ファイル配置、IF取り込みまでを一連の処理として併せて確認したいです。ただし、当日に形式差異が見つかると後続試験へ影響するため、最新のレイアウト定義だけは事前に共有をお願いします。\n**お客様：**\n> 分かりました。変換後ファイルを事前に用意できるか確認し、難しければ疎通試験でETLから連携します。\n**IF担当：**\n> ありがとうございます。どちらの方法になるか分かり次第、ご連絡ください。こちらでも両方の手順を準備しておきます。\n## よくある中式・不自然な表現の修正\n### 1. 「数据上应该没问题」を曖昧に言う\n△ データには問題ないと思います。\n✅ 社員番号やコード値など、データ内容には現時点で問題は見当たりませんでした。  \n✅ 内容面は確認済みですが、ファイル形式は別途確認が必要です。\n「問題ないと思います」没有说明确认范围和确定程度。应区分业务值、格式与读入结果。\n### 2. 把“转换前的文件”说成不自然的被动表达\n❌ これはETLに変換される前のファイルです。\n✅ これはETL変換前のファイルです。  \n✅ 今回受領したのは、ETLによる前処理が行われる前の形式です。\n「変換される前」并非语法错误，但技术说明中「ETL変換前」更简洁自然。\n### 3. 直接说「格式不一样」\n△ ファイルの形式が違います。\n✅ 今回のファイルは変換前レイアウトで、IFが想定している変換後の項目構成とは一致していません。\n应该说明比较的两个对象、具体差异以及为什么会影响测试。\n### 4. 「能给一下转换后的文件吗」直译\n❌ 変換したあとのファイルをくれますか。\n✅ お手数ですが、ETL変換後のファイルをご提供いただけますでしょうか。  \n✅ 一件だけでも、変換後サンプルをご共有いただくことは可能でしょうか。\n客户沟通中使用「ご提供」「ご共有」比「くれる」合适，也可通过限定一件样本来降低对方负担。\n### 5. “还是疏通时一起测试”说得不清楚\n❌ それとも、疎通のとき一緒にテストしますか。\n✅ 事前提供が難しい場合は、疎通試験でETL変換からIF取り込みまでを一連の処理として確認する形でも問題ございません。\n「一緒に」没有说明哪些处理在一起、由谁操作以及要验证到哪里。\n### 6. 把“未发现问题”等同于“测试通过”\n❌ データを見たので、テストはOKです。\n✅ データ内容の事前確認は完了しましたが、変換後形式での取り込み試験は未実施です。\n查看样本只能证明已完成静态检查，不能证明 ETL 和 IF 的实际处理成功。\n## 口頭・作文練習\n请用日语写一封约200～280字的客户确认邮件，包含以下信息：\n- 已确认收到的测试数据\n- 员工编号和业务代码等内容没有发现明显问题\n- 但文件是 ETL 转换前格式\n- IF 实际读取的是 ETL 转换后的文件\n- 需要确认转换后的字段名、字段顺序、分隔符和空值表达\n- 希望在下周连通性测试前收到一份转换后样本\n- 一条数据也可以\n- Group 侧也希望发送前确认\n- 如果事前提供困难，也可以在连通性测试中从 ETL 到 IF 一并验证\n- 希望对方告知采用哪种方式\n请至少使用以下四个表达：\n- 変換前／変換後\n- 受信形式\n- 項目構成\n- 取り込み可否\n- 再提供\n- ～に沿った\n- ～ないことには\n- ～に先立って\n- ～ていただくことは可能でしょうか\n- 併せて確認する\n## 参考答案\n> いつもお世話になっております。ご提供いただいたテストデータを確認し、社員番号や業務コードなど、内容面では現時点で大きな問題は見当たりませんでした。ただし、今回のファイルはETL変換前の形式であり、IFが実際に読み込む変換後ファイルとは項目構成が異なります。変換後の項目名、項目順、区切り文字および空項目の表現を確認しないことには、取り込み可否を判断できません。来週の疎通試験に先立って、一件だけでも入力仕様に沿った変換後サンプルをご提供いただくことは可能でしょうか。Group側でも送信前に確認したいとのことです。事前提供が難しい場合は、疎通試験の中でETL変換からIF取り込みまでを併せて確認する形でも問題ございません。どちらの方法で進めるか、ご連絡いただけますと幸いです。\n## 今日のポイント\n- 测试数据确认应分成：**业务内容 → 文件格式 → 实际读入**\n- 「内容面では問題がない」不等于「接口测试已经通过」\n- 说明格式差异时列出字段名、顺序、分隔符、编码和空值表达等具体项目\n- 如果需要改变内容或格式，应使用「再提供」；同一文件再次发送才更接近「再送」\n- 请求样本时说明用途，并可限定为一件数据，降低对方准备成本\n- 无法事前提供时，可提出 ETL 到 IF 的一体化验证作为代替方案\n- 替代方案仍应明确执行顺序、确认范围和发现问题时的处理方式\n- 「～ないことには判断できない」适合说明缺少必要材料时无法完成的判断",
  },
  "en-reliable-file-polling-message-metadata": {
    title: "Designing Reliable File-Polling and Message-Metadata Boundaries",
    sourceId: "3c8d34cbae198115af6fe8e6f9d3a844",
    markdown: "Today’s focus: preventing stale filenames and message metadata from being mistaken for evidence that a new file was received.\n## Architecture Review\n**Less precise**\n> If `CamelFileName` has a value, the SFTP poll must have found a file.\n**Senior-architect phrasing**\n> A populated filename header proves only that the message currently carries that metadata. It does not, by itself, prove that the current polling cycle retrieved a new remote file.\n> Before approving the flow, we should define the polling result contract, message-creation boundary, metadata lifecycle, duplicate-detection policy, empty-poll behavior, and observability signals.\n> An empty poll is a normal operational outcome. It should not reuse a previous exchange, retain stale file headers, or enter business processing as if a file had been consumed.\n## Technical Design Discussion\n> Distinguish trigger metadata from file-consumption evidence. A scheduled execution identifier and polling timestamp describe the attempt; a remote path, size, checksum, and stable intake record describe the retrieved file.\n> Initialize or clear file-scoped headers at the start of each logical processing boundary. Do not depend on the absence of cleanup in a reused local process or message branch.\n> Persist an intake record before irreversible processing when duplicate delivery is possible. Use a stable key such as source, remote path, size, modification time, and preferably a checksum where the protocol and volume permit it.\n> Route an empty poll explicitly. Record that the poll completed successfully with zero files, then stop before parsing, transformation, or notification logic that assumes a body exists.\n> Define post-processing behavior—move, rename, delete, or retain—and make it atomic enough that a crash does not cause uncontrolled reprocessing.\n## Incident / Debugging\n**Less natural**\n> SFTP did not get a file, but the filename still exists, so the adapter is wrong.\n**More natural**\n> The current poll returned no new file, but the downstream exchange still contained a filename header. We need to determine whether the header was inherited from an earlier processing step, copied during message reuse, or set by test data.\n> Let’s compare the exchange ID, polling timestamp, body size, remote-file event, header mutation history, local-process boundaries, and adapter trace before assigning causality.\n> The immediate mitigation is to guard business processing with explicit file-retrieval evidence and clear stale file-scoped metadata at the boundary. We should preserve the failing message trace for reproduction.\n## Stakeholder Update\n> We identified a metadata-handling issue in the SFTP intake flow. Under an empty polling cycle, a filename from an earlier message could remain visible downstream and be mistaken for a newly retrieved file. No new remote file was consumed in those runs. We have added an explicit empty-poll gate and are validating metadata reset and duplicate-processing controls before resuming normal operation.\n## Code Review / Mentoring\n> This condition checks only whether `CamelFileName` is non-empty. Add evidence that the current exchange actually contains a newly retrieved file, such as a validated body and intake event.\n> This shared local process mutates file headers without declaring ownership. Pass a typed file context or reset the headers when entering and leaving the process.\n> Avoid using the filename alone as a duplicate key. The same name may be reused, while the same content may arrive under a different name.\n> This parser runs on an empty body and fails with an XML end-of-file error. Route the empty-poll outcome before the parser instead of catching the parsing exception later.\n> Add tests for zero files, one file, multiple files, reused filenames, duplicate content, partial uploads, process restart, and failure after remote retrieval.\n## Corrections\n❌ The filename header means a file was downloaded.\n✅ The filename header is message metadata; confirm that the current polling cycle produced a file-retrieval event and a valid payload.\n❌ No file is an error from SFTP.\n✅ An empty poll is normally a successful polling outcome with zero files, unless the business schedule requires a file by a specific deadline.\n❌ Clear every header before processing.\n✅ Reset only the file-scoped metadata owned by this boundary, while preserving correlation and tracing fields required downstream.\n❌ Use the filename to prevent duplicate processing.\n✅ Use a stable intake identity based on the source and file attributes, supplemented by a checksum when appropriate.\n## Vocabulary Notes\n- **polling cycle**：轮询周期；连接远端并检查是否有新文件的一次执行\n- **empty poll**：空轮询；轮询成功，但没有发现符合条件的文件\n- **message metadata**：消息元数据；文件名、路径、时间戳等描述消息的信息\n- **stale header**：陈旧消息头；来自之前处理、已不代表当前事实的 Header\n- **message-creation boundary**：消息创建边界；新消息或新 Exchange 应从何处开始的明确位置\n- **file-scoped metadata**：文件级元数据；仅对当前文件有效、处理结束后不应继续沿用的信息\n- **retrieval evidence**：读取证据；证明当前周期确实取得文件的事件、记录或有效载荷\n- **intake record**：接收记录；系统登记某个文件已进入处理链路的持久化记录\n- **duplicate detection**：重复检测；识别同一文件或内容是否已被处理\n- **checksum**：校验和；根据文件内容计算的摘要，可辅助识别重复或损坏\n- **partial upload**：未完成上传；消费者读取时，生产方尚未完成文件写入\n- **post-processing policy**：后处理策略；成功或失败后移动、改名、删除或保留文件的规则\n- **header mutation history**：Header 变更轨迹；记录消息头在流程中何时、被哪个步骤修改\n- **empty-body guard**：空 Body 防护；在解析前明确阻止无有效载荷的消息继续处理\n## Speaking Drill\n用约 45 秒回答：\n> An SFTP-triggered iFlow sometimes reaches downstream processing with `CamelFileName` populated even though no new file was found. How would you investigate and redesign the boundary?\n**Model answer**\n> I would not treat the filename header as proof of retrieval. First, I would correlate the exchange ID, polling timestamp, body size, adapter event, and header mutation history to determine whether the value came from the current poll or an earlier message boundary. I would then model an empty poll as an explicit successful outcome with zero files and stop it before parsing. File-scoped headers should be initialized at the intake boundary, while correlation metadata should be preserved. For actual files, I would create a stable intake record and use source, path, file attributes, and a checksum where appropriate for duplicate detection. Finally, I would test empty polls, reused filenames, partial uploads, restarts, and failures after retrieval.",
  },
} as const;
