export const architectureTutorials: Record<string, string> = {
  "ccar-p-001-discovery-scoping": `## 深入教程
### 1. 先定义决策，再定义模型
业务方通常会用“做一个 Claude 助手”“自动化审批”“降低客服成本”描述需求。这些都是方向，不是可实现的需求。架构师首先要建立 **decision boundary（决策边界）**：系统在什么条件下可以只提供信息、提出建议、生成草稿、调用工具或真正改变业务状态。

建议使用八问发现法：
1. **Actor / 使用者**：谁发起任务，谁消费结果，谁承担最终责任？
2. **Job / 任务**：用户真正要完成的业务工作是什么？
3. **Input / 输入**：数据从哪里来，是否完整、可信、可授权？
4. **Output / 输出**：文本、结构化字段、建议、工具调用还是业务状态变更？
5. **Risk / 风险**：错误结果带来返工、经济损失、合规事件还是人身影响？
6. **Constraint / 约束**：时延、成本、地域、隐私、审计和现有系统限制是什么？
7. **Success / 成功**：正确率、完成率、人工节省、采纳率、升级率如何计算？
8. **Owner / 负责人**：谁批准上线、处理告警、维护知识并接受剩余风险？

### 2. 建立任务—能力矩阵
把完整业务过程拆成原子任务，再为每项选择最合适的执行机制：
- 明确规则、精确计算与权限判断：使用 **deterministic code（确定性代码）**。
- 分类、抽取、总结、改写与语义匹配：使用 Claude，但要求结构化输出和评估。
- 最新事实与企业知识：通过检索或工具取得，不能依赖模型参数记忆。
- 高风险、低置信度或不可逆决定：使用 **human review（人工复核）**。

这一步的关键不是让 Claude 做得最多，而是把非确定性限制在能被验证和补偿的位置。例如合同审核可以让 Claude 提取条款、匹配规则、给出风险说明，但“是否签署”仍由法务决定。

### 3. 从需求形成参考架构
一份最低可用的 **reference architecture（参考架构）** 应包含：
- Channel：Web、API、批处理或开发者入口。
- Identity：用户身份、服务身份、租户与数据范围。
- Orchestrator：状态、路由、超时、重试和人工升级。
- Claude：模型、系统提示、输出契约与版本。
- Context：会话状态、检索结果、文档和缓存。
- Tools：读取与写入能力、参数 schema、授权策略。
- Safety：输入筛查、输出验证、审批与失效关闭。
- Evals：离线数据集、在线反馈和发布门禁。
- Operations：日志、追踪、成本、SLO、告警与运行手册。

### 4. POC 应验证风险，而不是演示功能
**Proof of Concept, POC（概念验证）** 的目标是减少不确定性。先列出假设并排序：价值不确定性、模型能力不确定性、数据不确定性、集成不确定性、安全与合规不确定性。选择失败代价最高或最可能推翻方案的假设进行实验。

合同场景的 POC 不应只展示“Claude 能总结合同”，而应使用真实分布的匿名样本测试：条款召回率、风险误报/漏报、证据引用、人工复核时间、单份成本与敏感信息处理。POC 结束时必须能做出继续、调整或停止的决策。

### 5. 场景推演
假设需求是“自动审核供应商合同并决定是否签署”。合理的范围重写是：
- Claude 提取指定条款并返回结构化字段。
- RAG 检索公司最新标准条款，返回来源和版本。
- 规则引擎检查金额、地区、数据处理和责任上限。
- Claude 生成带证据的风险摘要，不直接批准合同。
- 缺少条款、证据冲突或高风险条件时转法务。
- 以条款召回率、严重风险漏报率、引用正确率和人工处理时间验收。

### 6. 考试常见陷阱
- 直接从模糊需求跳到模型选型。
- 把 HTTP 200、语句流畅或一次演示成功当作业务验收。
- 只定义理想路径，没有拒答、升级和停止条件。
- 用提示代替权限、规则或合规控制。
- POC 只验证容易成功的样本，没有覆盖最危险的失败模式。

## 练习参考答案
“用 Claude 自动审核合同”可以改写为：法务人员上传已授权合同；系统提取指定条款并匹配当前政策；输出结构化条款、风险等级、证据引用和不确定项；系统不得签署、发送或修改合同；严重风险、证据缺失和低置信度必须转人工；以严重条款召回率、引用正确率、人工复核时间、p95 延迟、单份成本和敏感数据泄漏为验收指标。`,

  "ccar-p-002-architecture-patterns": `## 深入教程
### 1. 三种基础形态
**Augmented LLM（增强型 LLM）** 是最小构件：一次或少量模型调用，加上检索、工具、记忆或示例。它适合单轮语义任务，通常比代理更便宜、更容易测试。

**Workflow（工作流）** 由应用代码决定路径。Claude 在固定节点执行分类、抽取、生成或评价。它的优势是可预测、可观测、易于设置门禁，适合审批、批处理和受监管流程。

**Agent（代理）** 由模型根据环境反馈动态决定下一步、选择工具并调整计划。它适合无法事先确定步骤数量的开放任务，但成本、延迟和错误传播都更高。官方架构原则是从最简单的有效方案开始，只有评估证明收益时才增加复杂度。

### 2. 五种组合模式
#### Prompt chaining / 提示链
把复杂任务拆成固定顺序的小任务，并在中间插入确定性 gate。例如：抽取需求 → 验证字段 → 生成设计 → 合规检查。它用更多延迟换取每一步更清晰的目标和更高可测性。

#### Routing / 路由
先分类，再进入专用提示、工具或模型。客服可以把一般咨询、技术故障、退款和安全事件分流。路由质量必须单独评估，因为错误路由会让后续组件再强也无法补救。

#### Parallelization / 并行化
**sectioning（分区）** 把独立子任务并行执行，例如安全、成本和可维护性分别评审；**voting（投票）** 对同一任务运行多个独立判断，以提高置信度。并行适用于互不依赖的任务，依赖链强行并行会产生不一致。

#### Orchestrator-workers / 编排者—执行者
中央模型动态拆解任务，工作模型分别执行，中央模型汇总。适合代码修改、研究和未知子任务数量的场景。必须限制任务数、共享上下文和汇总预算。

#### Evaluator-optimizer / 评估者—优化者
一个模型生成，另一个依据明确 rubric 评价并给出反馈，循环直到达标或达到上限。只有当评价标准清晰且迭代确实改善结果时才值得使用。

### 3. 选择模式的六个问题
1. 路径能否在设计时枚举？能则优先 workflow。
2. 中间结果是否能被程序或人验证？不能验证的长代理循环风险很高。
3. 子任务是否独立？独立才适合并行。
4. 错误是否可逆？不可逆动作放在代理边界外。
5. 是否存在明确完成标准？没有停止条件的代理可能持续消耗预算。
6. 复杂模式相对单次调用是否在 eval 中显著改善结果？

### 4. 代理循环的生产边界
一个可控代理循环至少包含：目标、当前状态、允许工具、每步环境反馈、最大轮数、token/费用预算、超时、重复检测、停止条件和人工升级。每次工具调用都应产生可追踪的观察结果，而不是让模型仅凭先前文本猜测成功。

对于不可逆动作采用 **propose-validate-execute（提议—验证—执行）**：Claude 只生成结构化意图；确定性代码验证权限、参数和政策；必要时由人批准；执行器调用真实系统并返回结果。

### 5. 两个对比案例
差旅报销步骤、阈值和审批人已知，适合 workflow：OCR/提取 → 规则校验 → Claude 解释异常 → 人工审批 → ERP 写入。开放式市场研究无法预先知道要搜索多少来源，适合 bounded agent：制定计划 → 搜索 → 阅读 → 判断证据缺口 → 继续或停止 → 引用汇总。

### 6. 考试常见陷阱
- 因为任务复杂就直接选择多代理。
- 用 autonomous agent 处理固定审批流程。
- 只设置最大轮数，没有业务完成条件。
- 把模型“说已完成”当作外部动作成功证据。
- 忽略每多一次调用带来的延迟、成本和累积错误率。

## 练习参考答案
员工报销应使用 deterministic workflow，在语言含糊处调用 Claude，并将 ERP 写入置于授权和审批之后。市场研究应使用 bounded agent，因为子任务和搜索次数依赖中间证据；同时限定来源类型、最大搜索次数、预算、引用要求和“证据充分”停止标准。`,

  "ccar-p-003-multi-agent-orchestration": `## 深入教程
### 1. 多代理解决什么问题
多代理的合理动机只有三类：专业能力隔离、权限/信任边界隔离，以及可以并行的独立工作。若只是提示很长、代码组织困难或希望“看起来更智能”，应先优化单代理与工具设计。

**Supervisor-worker（监督者—执行者）** 由 supervisor 规划、分配、验证并合并结果。**Handoff（交接）** 把控制权转给另一专业代理。**Review/debate（复核/辩论）** 让独立代理从不同标准检查同一结果。无论哪种模式，必须指定最终决策者，避免代理相互转交或同时修改状态。

### 2. 编排层的责任
编排层而非代理应拥有：
- 全局任务 ID、用户身份、租户和策略上下文。
- 状态机与每个子任务的状态。
- 并发、超时、重试、取消、预算和停止条件。
- 工具凭据的绑定与最小权限。
- 结果合并、冲突处理和人工升级。
- 审计记录、成本归因和恢复点。

代理只接收完成当前任务所需的最小上下文。不要让每个代理都看到所有会话、工具和敏感数据。

### 3. 设计 handoff contract
代理间交接应使用结构化契约，至少包括：
- **task_id**：稳定任务标识。
- **objective**：当前子任务，不是整个项目的模糊目标。
- **inputs** 与 **constraints**：允许使用的事实和边界。
- **evidence**：来源、工具结果、版本或引用。
- **result**：符合 schema 的输出。
- **confidence** 与 **uncertainties**：置信与未知项。
- **status** 与 **error**：成功、部分成功、阻塞或失败。
- **recommended_next_action**：建议，不是未授权执行。

传递自由文本聊天记录会制造歧义、上下文膨胀和提示注入传播。结构化契约便于验证、重放和追踪。

### 4. 状态与记忆
区分 **working state（工作状态）**、**conversation memory（对话记忆）** 和 **business record（业务记录）**。工作状态可以短期保存；对话记忆要有作用域、保留期与用户控制；业务记录必须由权威系统维护。代理共享的长期记忆容易出现陈旧、越权和跨用户污染，不能替代数据库事实。

### 5. 失败与恢复
假设四个 worker 并行完成合同提取，第三个失败。系统不应让其他代理猜测其结果。编排器记录部分完成，按幂等键重试第三项；若超过预算，输出明确缺口并转人工。写工具必须支持幂等或状态查询，避免因超时重复创建业务对象。

评估时除最终正确率外，还要测量：分解合理性、路由准确率、handoff 完整率、重复/循环率、工具成功率、冲突率、人工接管率、端到端延迟与成本。

### 6. 合同分析案例
监督者接收合同并创建任务；提取代理只读取文档并输出条款；政策代理只检索当前标准；风险代理比较两者；法务界面展示证据与差异。任何代理都没有签署或发送合同权限。监督者发现证据冲突时不会投票决定事实，而会保留冲突并升级法务。

### 7. 考试常见陷阱
- 所有代理共享管理员凭据和全部工具。
- 让 worker 直接互相调用，编排器失去全局状态。
- 用多数投票解决权威数据冲突。
- 没有幂等键、取消传播和部分完成恢复。
- 只评估最终答案，不评估路由、交接和成本。

## 练习参考答案
合同系统由 supervisor 持有任务状态和预算；提取代理只读合同，政策代理只读知识库，风险代理只接收结构化条款与政策证据，法务拥有最终决策。handoff 包含来源、版本、置信度和未知项。任何写入或外发动作都在代理外经过权限、规则与人工审批。`,

  "ccar-p-004-model-selection": `## 深入教程
### 1. 以工作负载选模型
官方模型选择框架的核心是 **capability（能力）**、**speed（速度）** 和 **cost（成本）**。架构师还应加入上下文、输出长度、工具可靠性、推理需求、部署平台、数据政策与生命周期。

先定义任务画像：输入类型与长度、输出契约、复杂度、风险、调用量、交互/批处理、并发与时延目标。再用代表性数据比较候选模型。不要从演示中的主观印象选型，也不要把“最强模型”作为默认答案。

### 2. 质量门槛与成本前沿
建立 **quality threshold（质量门槛）**：满足门槛的最经济方案才有资格进入比较。成本计算必须包括输入、输出、缓存写入/读取、多轮代理调用、检索、工具、重试和人工复核，而不只是单次 token 单价。

有些优化属于 **free wins（不损伤质量的收益）**：删除无关上下文、稳定前缀缓存、批处理可等待任务、修复重复调用。另一些属于真实 trade-off：更小模型、降低推理 effort、缩短输出或限制步骤。这些必须重新运行评估。

### 3. 模型路由
常见路由信号包括任务类别、风险等级、输入长度、工具复杂度、第一次结果的验证分数和服务等级。推荐流程是：
1. 确定性规则先拦截无需模型的请求。
2. 低风险、常见任务进入满足门槛的快速模型。
3. 复杂或异常任务升级到更高能力模型。
4. 高风险、证据不足或低置信度进入人工。

路由器本身是生产组件，需要独立数据集测量误路由率。不要用模型自报置信度作为唯一升级依据，应结合验证器、证据覆盖和历史表现。

### 4. 版本、别名与迁移
模型能力、参数和生命周期会变化。业务配置应使用可追踪的模型标识和提示版本；在需要稳定行为时使用固定快照或受控版本策略。迁移流程包括兼容性分析、离线回归、工具与 schema 测试、影子流量、灰度、监控和回滚。

截至课程更新时，Claude 产品线仍体现“高能力、平衡、低延迟”三个常见定位，但考试更关注选择方法和 trade-off，而不是死记某个 SKU。任何具体上下文长度、价格和可用性都应在实施时从 Models API 与官方模型页重新确认。

### 5. 选型矩阵示例
邮件分类：任务短、低风险、可用规则验证，选择满足准确率门槛的低延迟模型。合规摘要：长文档、证据要求高，选择长上下文与推理更强的模型，并保留人工复核。自主运维：工具复杂且动作高风险，模型能力只是条件之一，更重要的是受限工具、验证器、沙箱和审批。

### 6. 考试常见陷阱
- 只比较基准分数或单次 token 价格。
- 认为 HTTP 200 或结构化 JSON 就代表业务正确。
- 让每个请求都走最强模型，没有路由与预算。
- 迁移时只改模型名，不回归提示、工具和停止原因。
- 把自报 confidence 当作已校准概率。

## 练习参考答案
矩阵至少包含质量门槛、输入/输出规模、工具复杂度、p95 延迟、吞吐、端到端成本、失败影响、人工路径和版本策略。邮件分类倾向快速经济模型；合规摘要倾向能力更强模型；自主运维采用高能力模型也必须置于沙箱、最小权限、确定性验证和审批之后。`,

  "ccar-p-005-prompt-context": `## 深入教程
### 1. 系统提示的职责
高质量 **system prompt（系统提示）** 应清晰定义：角色、业务目标、适用范围、不可做事项、信息优先级、工具使用原则、输出 schema、拒答/升级条件与必要示例。不要把经常变化的用户数据、整份知识库或秘密写进系统提示。

可以用七段结构编写：Context、Role、Task、Rules、Evidence、Output、Fallback。指令之间出现冲突时应明确优先级。对工具调用使用结构化 schema 与执行层验证，不能指望一句“请勿越权”代替授权。

### 2. Zero-shot 与 Few-shot
先用清晰的 **zero-shot（零样本）** 指令建立基线。只有在输出边界、分类定义或特殊格式难以仅用说明表达时才加入 **few-shot（少样本）**。示例应覆盖易混类别和边界，而不是重复最简单的正常案例。

示例也是上下文中的数据：必须去除敏感信息、保持标签正确，并与当前政策一致。大量示例可能造成 token 成本、过拟合和注意力稀释。

### 3. Context engineering
上下文工程解决“在正确时刻提供正确信息”。把内容分为：稳定策略、当前用户请求、会话状态、检索证据、工具结果和历史摘要。每类内容都有来源、版本、信任级别与生命周期。

长上下文不是越长越好。随着上下文增长，相关信息可能被淹没并出现 **context rot（上下文退化）**。对长会话使用摘要、结构化状态、context editing 或 compaction；对知识库使用检索；发送前用 token counting 检查预算。

### 4. Prompt caching
提示缓存复用完全相同的前缀。正确布局是：工具定义与稳定系统规则 → 稳定文档/示例 → 逐渐变化的会话 → 当前请求。变化越频繁的内容越靠后。

可使用自动缓存或显式 **cache_control** 断点，并根据复用窗口选择 5 分钟或 1 小时 TTL。缓存命中依赖前缀一致；模型、工具选择、thinking/effort 或早期内容变化都可能使后续缓存失效。监控 **cache_creation_input_tokens** 与 **cache_read_input_tokens**，用真实命中率验证收益。

### 5. Skills、模板与工具的边界
**Prompt template（提示模板）** 适合参数化文本任务。**Agent Skill（代理技能）** 是包含说明、脚本与资源的可复用包，适合组织级工作方法和复杂产物流程。**Tool（工具）** 连接实时外部能力或执行动作。Skill 可以指导何时调用 MCP，但不能替代外部授权与数据获取。

企业推广 Skill 时需要来源审查、版本、评估、权限、分发、退役和 owner。Skill 中引用 MCP 工具应使用完全限定名称，避免工具解析歧义。

### 6. 8,000-token 策略案例
把固定政策放在稳定前缀并设置缓存断点；把用户资料、当前问题和实时检索结果放在后部。缓存不会改善错误政策，也不会减少输出 token，因此仍需内容审计和输出上限。若政策频繁变化，应按版本生成新前缀并接受首次缓存写入成本。

### 7. 考试常见陷阱
- 把所有历史消息无限追加到上下文。
- 用更多示例掩盖不清晰的分类定义。
- 把动态时间戳放在稳定前缀前部，导致每次缓存失效。
- 认为缓存保存“答案”或跨不同前缀模糊匹配。
- 用 Skill 替代实时工具、权限或业务状态。

## 练习参考答案
稳定前缀应包含工具定义、系统角色、政策与固定示例；动态后缀包含会话摘要、用户输入与实时结果。在稳定内容末尾设置缓存断点，记录缓存写入、读取、总输入、延迟与费用，并以相同评估集确认重构前后质量不下降。`,
};
